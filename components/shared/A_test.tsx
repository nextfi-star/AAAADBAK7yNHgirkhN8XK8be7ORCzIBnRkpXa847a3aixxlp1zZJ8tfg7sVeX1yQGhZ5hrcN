import {
	ChartPie,
	Earth,
	IdCard,
	MonitorCog,
	Settings,
	ShieldCheck,
	User,
	UsersRound,
	Wallet,
} from 'lucide-react'
import React from 'react'

const A_test = () => {
	return (
		<div>
			<ChartPie strokeWidth={1} />
			<User strokeWidth={1} />
			<ShieldCheck strokeWidth={1} />
			<IdCard strokeWidth={1} />
			<Earth strokeWidth={1} />
			<Settings strokeWidth={1} />
			<Wallet strokeWidth={1} />
			<MonitorCog strokeWidth={1} />
			<UsersRound strokeWidth={1} />
		</div>
	)
}

export default A_test
